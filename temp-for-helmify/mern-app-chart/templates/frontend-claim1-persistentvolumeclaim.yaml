apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "mern-app-chart.fullname" . }}-claim1
  labels:
    io.kompose.service: frontend-claim1
  {{- include "mern-app-chart.labels" . | nindent 4 }}
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.pvc.claim1.storageRequest | quote }}
